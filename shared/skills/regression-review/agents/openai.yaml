interface:
  display_name: "Regression Review"
  short_description: "Behavior-path regression audit"
  default_prompt: "Use $regression-review to perform a scoped, behavior-graph-led audit of the current changes and write a Markdown report with findings, intentional changes, coverage gaps, and a gate recommendation."

policy:
  allow_implicit_invocation: false
