interface:
  display_name: "Coverage-First Debug & Repair"
  short_description: "Batch breakpoints, prove, and repair runtime bugs"
  default_prompt: "Use $debug to install the full safe first-pass native-breakpoint batch before continuing, preserve every accepted structured-probe occurrence as a persisted NDJSON record, then prove, repair, verify, and clean up the bug."
