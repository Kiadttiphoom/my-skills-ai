interface:
  display_name: "Receiving Thermo Review"
  short_description: "Resolve structural reports without regressions"
  default_prompt: "Use $receiving-thermo-review to consume a thermo report, verify every structural item and behavior-parity risk, resolve 350-line findings through cohesive boundaries, and apply scoped fixes without touching Git staging."

policy:
  allow_implicit_invocation: false
