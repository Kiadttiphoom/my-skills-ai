# Copy to .env in the project directory and fill locally.
# Never commit or send this file after adding real credentials.
USEAPI_TOKEN=

# Optional when only one Google Flow account is connected.
GOOGLE_FLOW_EMAIL=
